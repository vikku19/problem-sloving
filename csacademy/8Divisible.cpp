#include<bits/stdc++.h>
using namespace std;
int main(){
    string str;cin>>str;
    sort(str.begin(),str.end());
    int n=str.size();
    do{
        if(str[])
    }while(next_permutation(str.begin(),str.end()));
}