#include<bits/stdc++.h>
using namespace std;
string sumitstring(string str){
    int n=str.size();
    for(int i=0;i<(int)str.size()-1;i++){
        if(i<(n-1)){
            if(abs(str[i]-str[i+1])!=1)
        }
    }
}
int main(){
    int t;string str;cin>>t;while(t--){
        cin.ignore();
        cin>>str;
    }
    return 0;
}