#include<bits/stdc++.h>
using namespace std;
int main(){
    string str,str1;
    getline(cin,str);
    getline(cin,str1);
    if(str.size()!=str1.size()){
        for(int i=0;i<str.size();i++){
            if(str[i])
        }
    }
}