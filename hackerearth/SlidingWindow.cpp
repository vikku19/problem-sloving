#include<bits/stdc++.h>
using namespace std;
int main(){
    string str,ptr;cin>>str>>ptr;
    for(int l=0,r=ptr.size()-1;r<str.size();l++,r++){
        for(int i=0;i<ptr.size();i++){
            if(str[i]!=ptr[i]){
                 
            }
        }
    }
}