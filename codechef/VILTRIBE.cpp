#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;cin>>t;while(t--){
        vector<pair<int,char>>v;
        string str;cin>>str;
        for(int i=0;i<str.size();i++){
            if(str[i]=='A'){
                v.emplace_back(make_pair(i,A));
            }else if(str[i]=='B'){
                v.emplace_back(make_pair(i,B));
            }
        }
        for(int i=0;i<n;i++){
            if()
        }
    }
}