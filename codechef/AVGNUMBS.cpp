#include<bits/stdc++.h>
using namespace std;
using ll=long long int;
using ld=long double;
int main(){
    int n;cin>>n;
    int a[n];
    double sum=0;
    for(int i=0;i<n;i++){
        cin>>a[i];
        sum+=a[i];
    }
    if()
    cout<<fixed<<setprecision(4)<<(ld)sum/n<<endl;
}
// TODO