#include<bits/stdc++.h>
using namespace std;
using ll=long long int;
vector<pair<int,int>>v;
int main(){
    int n;cin>>n;while(n--){
        int a,b;cin>>a>>b;
        v.emplace_back(make_pair(a,b));
    }
    sort(v.begin(),v.end());
    for(int i=0;i<v.size();i++){
        
    }
}