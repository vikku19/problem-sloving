#include<bits/stdc++.h>
using namespace std;
using ll=long long int;
using ld=long double;
bool CheckSqrt(ll n){
    ld p=sqrt(n);
    ll q=sqrt(n);
    if((p-q)!=0)
       return true;
    return false;
}
int main(){
    int t;
    cin>>t;
    while(t--){
        ll n,sum=0;
        cin>>n;
        sum=
    }
}