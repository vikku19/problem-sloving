#include<bits/stdc++.h>
using namespae std;
int main(){
    vector<pair<int,int>>v;
    int n,x;cin>>n>>x;while(x--){
        int a,b;cin>>a>>b;
        v.emplace_back(make_pair(a,b));
    }
    int p=v[0].first;
    int q=v[0].second;
    for(int i=0;i<n;i++){
        if(v[i].first<=p){
            p=
        }
    }
}