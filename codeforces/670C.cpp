#include<bits/stdc++.h>
using namespace std;
using ll=long long int;
int main(){
    int n,m;cin>>n;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    cin>>m;
    vector<pair<int,int>>v;
    for(int 0;i<m;i++{
        int a,b;
        cin>>a>>b;
        v.emplace_back(make_pair(a,b));
    }
    
}