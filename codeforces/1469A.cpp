#include<bits/stdc++.h>
using namespace std;
int min(){
    int n;string str;cin>>n;while(n--){
        cin>>str;
        stack<char>stk;
        for(int i=0;i<(int)str.size();i++){
            if(str[i]=='(' || str[i]=='?'){
                stk.push(str[i]);
            }

            
    }
}